apiVersion: v1
kind: Service
metadata:
  name: ecommerce-service
  namespace: ecommerce
spec:
  type: ClusterIP
  selector:
    app: ecommerce
  ports:
    - name: http
      port: 8090
      targetPort: 8090
